{
  "name": "personal-website",
  "version": "1.0.0",
  "description": "Joel Weinberger's personal website - static site generator",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist",
    "rebuild": "npm run clean && npm run build",
    "serve": "npm run build && cd dist && python3 -m http.server 8080"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "marked": "^11.1.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}
